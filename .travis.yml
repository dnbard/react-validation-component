language: node_js
node_js:
  - "node"
install:
  - npm install -g codecov istanbul
script:
  - istanbul cover ./node_modules/.bin/_mocha tests/*.spec.js --reporter lcovonly -- -R spec --require babel-core/register
  - codecov